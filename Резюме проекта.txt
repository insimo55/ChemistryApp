Резюме проекта: Система учета химических реагентов
Обзор проекта
Создано полнофункциональное веб-приложение для учета и управления движением химических реагентов на различных объектах (склады, скважины). Приложение построено на современном технологическом стеке, следует лучшим практикам веб-разработки и готово к развертыванию в production-среде.
Архитектура представляет собой классическое одностраничное приложение (SPA) с разделением на независимые бэкенд и фронтенд, что обеспечивает гибкость, масштабируемость и простоту поддержки.
📁 Бэкенд (Django + Django REST Framework)
Бэкенд является "мозгом" системы. Он отвечает за хранение данных, реализацию бизнес-логики и предоставление защищенного API для взаимодействия с клиентской частью.
Ключевые компоненты:
Модели данных (Django ORM):
User: Кастомная модель пользователя с ролями (admin, engineer, logistician) для реализации ролевой модели доступа.
Facility: Справочник объектов (склады, скважины) с указанием типа и местоположения.
Chemical: Справочник химических реагентов с единицами измерения.
Inventory: Таблица для хранения текущих остатков реагентов на каждом объекте. Является "срезом" данных на текущий момент.
Transaction: Ядро учетной системы. Неизменяемый журнал всех операций (поступление, списание, перемещение). Каждая транзакция имеет уникальный ID операции (operation_uuid), что позволяет группировать связанные записи.
API (Django REST Framework):
Реализованы RESTful эндпоинты для CRUD-операций со всеми основными сущностями (/api/users/, /api/facilities/, /api/chemicals/).
Созданы специализированные эндпоинты для бизнес-логики:
/api/operations/create/bulk/: Для создания массовых операций.
/api/operations/edit/: Для корректного "бухгалтерского" редактирования операций (сторнирование + создание новой).
/api/operations/delete/: Для безопасного удаления операций с пересчетом остатков.
Разработаны аналитические эндпоинты для построения отчетов (/api/reports/...).
Бизнес-логика:
"Оперативный" учет: Реализована логика, позволяющая проводить операции "задним числом" и иметь отрицательные остатки.
Автоматический пересчет: После каждой операции (создания, изменения, удаления) запускается полный пересчет остатков для затронутых позиций, что гарантирует целостность данных в Inventory.
Валидация: На уровне API реализованы проверки данных (например, нельзя списать с несуществующего склада) и прав доступа (инженер может списывать только со своего объекта).
Обработка времени: Корректно обрабатываются часовые пояса, что позволяет пользователям вводить локальное время операции.
Аутентификация и права доступа:
Используется JWT (JSON Web Token) для безопасной аутентификации.
Реализован механизм автоматического обновления access_token с помощью refresh_token, обеспечивая бесшовную сессию для пользователя.
Настроены детальные права доступа на уровне API, разграничивающие возможности разных ролей.
Админ-панель:
Настроена стандартная админка Django для прямого управления данными, что удобно для администрирования и отладки.
🌐 Фронтенд (React + Vite + Tailwind CSS)
Фронтенд — это современный, быстрый и отзывчивый пользовательский интерфейс, который обеспечивает удобное взаимодействие с системой.
Ключевые компоненты:
UI/UX:
Интерактивный дашборд: Главная страница отображает список всех объектов с ключевой информацией (количество позиций, общий вес/объем).
"Drill-down" аналитика: Реализована возможность "проваливаться" в данные: с дашборда на страницу объекта, со страницы объекта к истории конкретного реагента, из истории к деталям конкретной операции.
Информативные формы: Форма проведения операций "умная" — она показывает текущие остатки, подсвечивает реагенты в наличии и позволяет создавать новые записи в справочниках "на лету" через модальные окна.
Продвинутая фильтрация: Страница истории операций оснащена гибкими фильтрами по дате, типу операции и объектам.
Удобные отчеты: На странице объекта реализован аналитический отчет за период с расчетом начального/конечного остатка и оборотов.
Архитектура:
Компонентный подход: Приложение разбито на переиспользуемые компоненты (формы, модальные окна, таблицы).
Роутинг: Используется react-router-dom для навигации между страницами и защиты роутов (нельзя попасть в систему без логина).
Управление состоянием: Применяется Zustand для глобального хранения данных аутентификации (токены, информация о пользователе).
Взаимодействие с бэкендом:
Используется axios с настроенными "перехватчиками" (interceptors), которые автоматически добавляют JWT-токен в каждый запрос и обрабатывают его обновление при истечении срока действия.
🧪 Стек технологий и развертывание (DevOps)
Проект полностью контейнеризирован с помощью Docker, что обеспечивает идентичность окружений для разработки и продакшена и значительно упрощает развертывание.
Бэкенд: Django, Django REST Framework, Gunicorn, PostgreSQL.
Фронтенд: React, Vite, Tailwind CSS, Axios, Zustand.
Аутентификация: JWT (djangorestframework-simplejwt, djoser).
Окружение:
Разработка: docker-compose для локального запуска всей связки.
Продакшен:
Бэкенд: Развернут на VPS (Beget) с Ubuntu. Используется docker-compose для оркестрации контейнеров backend (Django+Gunicorn) и db (PostgreSQL).
Фронтенд: Развернут на специализированном хостинге для статических сайтов (Vercel), что обеспечивает высокую скорость загрузки, CDN и автоматический HTTPS.
Безопасность: Настроены ALLOWED_HOSTS и CORS для защиты бэкенда, используется HTTPS для шифрования трафика.
